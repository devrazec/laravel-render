# laravel-render
Laravel 11 with a Docker PHP Image

Source: https://github.com/render-examples/php-laravel-docker

# Commands

```
composer update
composer install
npm i

php artisan key:generate
php artisan key:generate --show

php artisan serve --port=8080

```